function stochVolSim = runModel(data)

% runs stochasticity-volatility model (main, lesioned)
% Created in June 2023

%% unpack data struct 
o           = data.outcome; 
x           = data.state;
tvolatile   = data.t(:,2);
tstable     = data.t(:,1);













end % end of function 